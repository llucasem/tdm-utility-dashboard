# Dependencies
node_modules/

# Environment variables — NEVER upload these
.env.local
.env*.local

# Next.js build output
.next/
out/

# Claude Code local config (contains API keys)
.claude/

# Internal working docs — not for client repos
CLAUDE.md
AGENTS.md

# Google OAuth credentials — NEVER upload these
google-oauth.json
client_secret*.json
"Secret Key.com.json"
Secret\ Key*.json

# Misc
.DS_Store
*.pem
npm-debug.log*

# Investigation outputs — contain sensitive Gmail data (subjects, senders, amounts)
investigation.txt
investigation.err
deep-investigation.txt
deep-progress.log

# Old client data exports
Utilities backup - Sheet1.csv

# Local DB backups — kept out of the repo because they contain client data
backups/

# Backfill / diagnostic logs
backfill.log

# Saved client emails and audit data exports — contain client billing data
*.eml
audit-gmail-sample-30d.json
airtable-rent-classification.json
audit-report-pipeline-2026-05-16.md
rent-mapping-review.csv
rent-mapping-review.xlsx
mensaje-para-jake.md

# Knowledge-graph output (local tooling)
graphify-out/

# Registro de cuentas propuesto (datos de cliente)
account-registry-propuesto.json

# Scripts temporales de analisis
_tmp*.mjs
